[project]
name = "ariadne-contract-tools"
version = "0.1.0"
description = "Local validation tools for the Ariadne design draft"
requires-python = ">=3.11"
dependencies = ["jsonschema>=4,<5"]

[tool.uv]
package = false
