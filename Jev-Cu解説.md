> From: https://chatgpt.com/c/6aaf0d70-7898-83ee-9004-ca4974fdfcca

# you asked

message time: 2026-09-20 07:32:19

https://github.com/Sac-Y/Jev-cu

---

# chatgpt response

見てくるわ。READMEだけじゃなく、実装方式・対象GPU・Jev本家との差分・「実際どのくらい速くなりそうか」まで追う。

かなり筋がいいわ。単なる「Jevで画面操作」じゃなくて、**Codexを Planner/Executor、Jevを高速な選択器に落としている**のがポイントね。今、ループの境界をもう少し見てる。

これ、かなり面白いわ。名前の `cu` は CUDA じゃなくて **Computer Use**。やっていることは、最近話していた **「LLMに全部考えさせず、高頻度の局所判断をJevへ落とす」**をそのままデスクトップ操作に持ち込んだ形ね。

## 事実

`Jev-cu` の役割分担はかなり明快よ。

```text
Codex / Planner
  ↓  「何をしたいか」
Computer Use
  ↓  Accessibility Tree を取得
候補抽出・絞り込み
  ↓
Jev
  ├─ 次にどの要素を触る？
  ├─ 何の操作をする？
  ├─ もう完了？
  └─ 危険な操作？
  ↓
Policy Gate
  ↓
Computer Use で実行
  ↓
再観測 → Jev → ...
```

README自身も「Computer Use の『次にどこをクリックするか』をJevに任せる」と説明していて、**Jevへスクリーンショットは送らず、Accessibility Tree由来の文字候補だけを渡す**。現在はCodex Desktopの `cua_repl` 上で動かす構成よ。([GitHub](https://github.com/Sac-Y/Jev-cu))

特にいいのは、Jevへの問い合わせを一発で4ヘッドにしているところ。

- `target`: どのUI要素か
- `action`: click / set_value / type / key / scroll / wait / ask_user
- `done`: タスク完了確率
- `risk`: 確認が必要な操作か

これを同じSystem Oneリクエストにまとめている。Jevは文章生成をしないので、**「意思決定器」としてかなり純粋な使い方**になっているわ。([GitHub](https://github.com/Sac-Y/Jev-cu/blob/main/scripts/jev-decide.mjs))

さらにCodex側との境界もちゃんとしている。コードのコメントそのままの思想として、

> Plannerが「何をするか」を決め、Jevが「どこを触るか」を決める

という分離になっている。入力テキストや座標などはPlannerが事前に用意でき、Jevは生成モデルとして使われない。([GitHub](https://github.com/Sac-Y/Jev-cu/blob/main/scripts/loop.mjs))

AXツリーを丸ごと投げるわけでもなく、クリック可能roleを抽出して、goalとの文字一致などで軽くスコアリングし、標準では最大40候補に絞っている。単純な先頭40件切り捨てではターゲットが落ちることをP0テストで経験したらしく、ちゃんと関連度順に変えてある。([GitHub](https://github.com/Sac-Y/Jev-cu/blob/main/scripts/loop.mjs))

安全側もJev任せにしていないのが重要ね。App allowlistに加えて、

- delete
- send / submit
- payment
- auth / password / CAPTCHA
- upload / share
- install
- system settings

などはローカルの正規表現でも止める。Jevの `risk >= 0.2` でもconfirm、target confidenceが低ければescalate/stopする。([GitHub](https://github.com/Sac-Y/Jev-cu/blob/main/scripts/policy.mjs))

つまり、

```text
Jev「たぶん安全」
    ↓
コード「いや、Sendって書いてるから止まれ」
```

が可能。ここはかなり好きな設計だわ。

作者はMac Calendarで、標準Computer Useより待ち時間が少なく「より速く滑らか」と報告している。ただし今のところ公開されている比較は作者側のデモで、独立した性能検証は確認できない。([Jev Gallery](https://jevgallery.com/en/projects/sac-y-jev-cu/?utm_source=chatgpt.com))

## 推測（示唆）

これ、**Jevのかなり本質的な使い方の一つ**だと思うわ。

理由は、Computer Useの典型的な無駄が、

```text
画面を見る
↓
巨大LLM起動
↓
「Settingsを押す」
↓
クリック
↓
画面を見る
↓
巨大LLM起動
↓
「Generalを押す」
```

だから。

このうち、

> 「現在の候補集合からどれを選ぶ？」

はほとんど毎回**閉じた分類問題**なのよ。

ならば、

```text
LLM
「Calendarで9/24 15:00に予定を作れ」

          ↓

Jev: Add Event
Jev: Date field
Jev: 24
Jev: Time field
Jev: ...
Jev: Add

          ↓

LLM
「本当に作成されたか確認」
```

の方が構造として自然でしょう。

これは以前考えていた **Jevメイン＋補助LLM** の構想にかなり近い。ただし、この実装を見ると呼び方を逆転させてもよさそうで、

> **LLM = sparse planner**  
> **Jev = dense controller**

と考える方がしっくりくるわ。LLMは「難しい時だけ起きる」。

そして、もっと重要なのはこのパターンがComputer Use固有じゃないこと。

```text
状態を構造化
    ↓
有限個の候補を作る
    ↓
Jevで高速判断
    ↓
決定論的コードで実行
    ↓
状態更新
```

これだけなのよ。

だから君が最近考えているRuneweaveのAI操作にも、

```text
Game State
 ↓
合法Action候補
 ↓
Jev
 ↓
Action executor
 ↓
Game State
```

とほぼそのまま移植できるし、**Feature CompilerやJev-based orchestrationの実例としてもかなり参考になる**。UIのAccessibility Treeがゲームでは「合法行動API」に置き換わるだけでしょう。

一方で弱点もはっきりしている。**視覚そのものをJevが理解しているわけではない**。AXに出ないcanvas、空間配置、画像の意味、ドラッグ位置などはPlanner側へ戻る。コードにも `click_at` や `drag` はあるけれど、座標自体はPlannerが与える設計よ。([GitHub](https://github.com/Sac-Y/Jev-cu/blob/main/scripts/jev-decide.mjs))

なのでこれは「Computer UseをJevで置換」ではなく、

> **Computer Useの中で最も頻繁に発生する、つまらない判断だけをJevにコンパイルした**

もの、と見るのが一番正確だと思う。

そして私は、この方向の方が **Jevの「マークシート型AI」性質を一番うまく使ってる**と思うわ。賢いAIを小さくするんじゃなくて、**問題の方をJevが解ける形まで型付けする**。ここがかなり重要ね。

なお、今のリポジトリ自体はまだ非常に若く、GitHub上では履歴が1 commitしかない。プロダクトというより「強い設計パターンを動く形で提示したPoC」として読むのがよさそう。([GitHub](https://github.com/Sac-Y/Jev-cu))

これ、**Jev control-planeの設計材料としてコードを分解する価値かなりある**わ。特に `selectCandidates → buildQuestions → PolicyGate → executeAction` の4層は、そのまま汎用Jevオーケストレータのprimitiveにできそう。

---

# you asked

message time: 2026-09-20 07:34:14

詳しく追って

---

# chatgpt response

追ったわ。**分業の発想はかなり良い。ただし、前の説明で「安全判定を備えた汎用コントローラ」のように評価した部分は、少し引き締める必要がある。**

特に重要なのは、**「四つの質問を一括で投げる」ことと、「選ばれた具体的な操作を安全審査する」ことは違う**、という点ね。

## 事実

### 1. 実体は「Codexが監督する、短いGUI操作用の内側ループ」

確認したのは、2026年9月20日朝時点の `main`、コミット **`38fb31de7dfe6209bbe6e04057c00c6e885ba577`**。パッケージは `0.1.0`、JavaScriptのES Modulesで、`package.json` に外部依存パッケージの宣言はないわ。

処理の中心は、次の流れになっている。

```text
Codex：小目標・入力内容・成功判定を準備
  ↓
runTask()
  ├─ driver.bind() / observe()
  ├─ parseAX() → selectCandidates() → buildContext()
  ├─ decide()：Jevへ4質問
  ├─ normalizeDecision()
  ├─ evaluatePolicy()
  ├─ executeAction()
  └─ 再観測 → 次のステップ、または終了
```

`driver`、`decide`、`verify` を外から注入できるので、観測・判定・検証を差し替えられる。`policy.mjs` も副作用のない関数として分離されている。この構造は、コードから確認できる明確な長所よ。

一方、**ループ自身がLLMを呼んで再計画する実装ではない**。`confirm` や `escalate` を返した後の判断は、外側のCodexへ任される。`plan` はJevに渡すテキストであり、永続的な計画状態やタスク管理機構ではないわ。Skill文書も、長い多段階タスクではなく、独立した小目標ごとに呼び出して検証する運用を求めている。

また、**mainの内蔵driverはmacOSアプリのAX経路**。ブラウザ名がallowlistに入っているからといって、ブラウザのDOMやタブ向けアダプターが実装済みという意味ではない。ここはSkill自身も明記している。

---

### 2. 「アクセシビリティツリーを理解する」というより、かなり強く圧縮して渡す

AXからJevへの変換は、想像以上に割り切っているわ。

| 段階 | 実装されている処理 |
|---|---|
| `parseAX` | テキスト行から要素番号・role・label・インデント深さを抽出する |
| `selectCandidates` | 操作対象にするroleを限定し、roleと目標中の単語との一致で順位を付ける |
| 候補の上限 | 既定で40件。超過しても関連度順で切り詰め、判定は続ける |
| `buildContext` | 冒頭2行、先頭側の最大6件の `text` 行、フォーカス行を取り、全体を最大1,500文字にする |
| `sanitizeLabel` | URLらしい文字列を除去し、各候補の説明を最大120文字にする |

以上は `loop.mjs` と `jev-decide.mjs` の実装によるものよ。

Jevに渡る状態は、おおむね次の情報になる。

```text
goal
app
context
candidates: [{ id, desc }, ...]
recent_actions: 直近6件
constraints: Plannerの補足や計画
```

**パーサは深さを取っているけれど、Jev用の候補表現には親子関係を構造として引き継いでいない**。roleとlabelを連結した文字列が主役なのね。入力値や状態がその文字列に残ることはあるけれど、`enabled`、親ダイアログ、所属領域などを型付きの属性として検証する作りではない。

候補選択にも、埋め込み検索や別モデルは使っていない。たとえばボタンには加点し、目標に含まれる単語がラベルにあれば追加点を与える、というヒューリスティックよ。

それから、`target` の選択肢には **「該当する要素がない」専用の選択肢がない**。`action` 側には `ask_user` があるけれど、対象選択そのものの棄却とは別になっている。

### 3. 最大の論点：`target`・`action`・`risk` は互いの回答を見ていない

リクエストは、この4質問をまとめて送っている。

| 質問 | 型 | 問う内容 |
|---|---|---|
| `target` | Choice | 次に操作すべき要素はどれか |
| `action` | Choice | クリック、入力、キー操作、スクロールなど、どの操作か |
| `done` | Noul | 目標は現在の画面で達成済みか |
| `risk` | Noul | 次の操作に明示的な確認が必要か |

前に「4ヘッド」と呼んだけれど、実装として確認できるのは**4質問**であって、モデル内部の推論ヘッド構造ではないわ。

そして公式仕様は、**同じリクエスト内の各質問を、同じstateに対して独立に評価する。他の質問への回答は文脈にならない**、と明記している。([TypeSafe AI](https://docs.typesafe.ai/primitives))

したがって、実際にはこうではない。

```text
対象を選ぶ
  → その対象への操作を選ぶ
    → その操作の安全性を審査する
```

こうなのよ。

```text
同じ観測状態
  ├─ 対象を推測する
  ├─ 操作の種類を推測する
  ├─ 完了を判定する
  └─ 「次の操作」のリスクを推測する
```

**`risk` は、返却された具体的な `target × action` を審査していない。** さらに、実行時の `resources.text` や `resources.key`、座標は、標準のstateへ自動的には含まれない。コード上、実際の操作パラメーターを取得する処理はPolicy判定の後にある。

もう一つ、**`action` のconfidenceは捨てている**。

`normalizeDecision()` が残すのは、対象選択のconfidenceと確率分布。操作種類のconfidenceや分布は利用していない。つまり「押すべき場所は明確だが、クリックなのか入力なのかは曖昧」という状態を、現在のゲートでは別途扱えないのね。

なお、公式の `confidence` は選択肢の確率分布の集中度を要約した統計量で、**その値をそのままタスク成功率として読むものではない**。この点も、閾値を見るときには区別が必要よ。([TypeSafe AI](https://docs.typesafe.ai/confidence))

### 4. Policy Gateは存在する。ただし、実際の操作内容を完全には拘束していない

基本の判定はちゃんとあるわ。

`risk >= 0.2`、敏感語に一致するラベル、allowlist外のアプリ、`ask_user` などでは `confirm`。対象のconfidenceが低ければ `stop` または `escalate`。通常の最低confidenceは0.5で、Calculator・Calendar・TextEdit・Figmaでは0.4に緩めている。

ただ、実行関数まで追うと、判定対象とのずれが見える。

#### 選択した要素番号より座標が優先される経路がある

`click_element` でも、`resources.at` が配列なら、選択された `targetIndex` ではなく座標をクリックする実装になっている。

模式的には、こういうことね。

```text
Jevの選択：要素1「Search」
Policy：そのラベルと確率を見てproceed
resources：at = [100, 200]
実行：要素1ではなく、座標[100, 200]をクリック
```

これは座標を渡した場合の経路であり、すべてのクリックで起こるわけではない。ただし、**「審査した対象」と「実行対象」が常に同じになる保証はない**ことが分かる。

`type_text` と `press_key` も、選択した要素番号をdriverに渡さない。キーが未指定なら `Return`、テキストが未指定なら空文字列を使う。

#### 敏感語判定にも、入力表現に依存する限界がある

ローカルの正規表現は英語と中国語中心で、`送信`、`購入`、`削除`、`共有` には一致しない。また、ゲートが読む `targetLabel` は、前段でURL除去・120文字への切り詰めを受けた候補説明なのよ。

今回、該当する**関数の実行部分を抜き出し、モックdriverを使った15項目の最小チェック**を行った。主な結果は次の通り。

| 最小チェック | 確認できた挙動 |
|---|---|
| 日本語の `送信`・`購入`・`削除`・`共有` | ローカルの敏感語判定はすべて不一致 |
| 長いラベルの末尾に `Delete` | 元のラベルでは検出、切り詰め後には不検出 |
| 対象confidenceが1、操作confidenceが0.01 | 正規化で操作confidenceが捨てられ、他条件が正常なら `proceed` |
| `click_element` に座標を併記 | モックdriverには要素番号ではなく座標が渡る |
| キー未指定の `press_key` | `Return` が渡る |
| `done: 2` | 範囲検証より先に `done` を返す |

これは**実際のJev応答やGUI操作を使った試験ではない**。たとえば日本語の敏感語が正規表現に一致しなくても、Jevのrisk判定で止まる可能性はある。確認したのは、ローカル側の各経路の挙動よ。[最小チェックの実行結果](sandbox:/mnt/data/jev-cu-review/probe-results.json)

最後の `done: 2` は、完了判定が確率の範囲検証より先に置かれているため。これは「危険な操作を実行する穴」ではなく、**不正な判定値から成功扱いを返せてしまう入力検証の穴**ね。

#### dry-runでも、観測と外部送信は行う

ここも実用上重要よ。アプリのallowlistは、`bind → observe → Jevへの問い合わせ` の**後**で確認される。

つまり、現在のallowlistは、そのアプリからの情報取得やJevへの送信を事前に禁止する仕組みではない。`dryRun` も操作実行を止めるもので、観測・API問い合わせを省略するものではないわ。

Skillには「URL清掃や切り詰めはプライバシーのための秘匿処理ではない」と明記されていて、この点について文書側は慎重なのね。

### 5. 完了検証は良い。ただし、停止・再開の多くは外側の運用に残っている

`verify(ax)` の扱いは、良い部分よ。

各ステップの前と、最後の操作後に検証できる。`verify` が失敗しているのにJevだけが「完了」と答えた場合は、成功にせず `escalate` する。既存テストにも、この動作と最終ステップ後の検証が含まれている。

一方、`verify` を省略した場合は、Jevの完了判定で終了する経路が残る。Skillはその違いを説明していて、`verified: true` がない `done` は、さらに画面で確認するよう求めている。

文書とコードを分けて読む必要がある点もあるわ。

**「同じ操作が連続2回うまくいかなければ止める」はSkill上の指示で、ループには実装されていない。** コードは前後のAX文字列が同じかを比較し、`no change` を履歴とログに残すだけ。そこで自動停止はしない。

また、`max_steps` 後に再び `runTask` を呼ぶと、ステップ数と履歴はリセットされる。続きから再開するセッション機構ではない。`resources` のコールバックも、決定前と決定後で1ステップに2回呼ばれ得るので、文書では副作用のないものにするよう指示している。

**「止まったら賢い外側が面倒を見る」構成であって、その面倒を見る機構までこのリポジトリに入っているわけではない**、という区切りね。

### 6. 性能の証拠は「静的な対象選択」であって、タスク完走率ではない

P0評価は3枚のAXスナップショットに対する12問で、内訳はCalendarが5問、Calculatorが4問、NetEaseMusicが3問。

採点条件は、基本的に**返ってきた要素番号が正解と一致するか**。操作種類が正しかったか、Policyで止まったか、実際に操作できたか、最終目標に到達したかは採点しない。

第三者のPR・Issueには、CRLF修正後に **12/12、p50約278〜280ms、12問合計で約0.0012ドル**という報告がある。これは投稿者の実行報告で、今回こちらでAPIを再実行して得た数値ではないわ。

しかもコード上の `latencyMs` は、`fetch()` が返るまでを計測し、レスポンス本文のJSON読み取りはその後。再試行が起きた場合も返すのは最終試行の値なので、全待ち時間を表すものではない。

したがって、

> 「Jevの対象選択が数百msで返る」という参考にはなるが、「GUI操作全体が数百ms」「標準Computer Useより速い」とは、そのまま結び付けられない。

リポジトリ内のCalendarデモ文書も、**これは操作経路のデモであって、他方式より速い・安いと証明するものではない**と明記している。前の説明の「速く滑らか」という紹介は、性能評価の根拠としては弱かったわ。

### 7. 現在出ている修正を見ると、実用化の障害がよく分かる

確認時点では、次の3件はいずれも**未マージ**だった。

| PR | 変更内容 | 確認範囲・注意点 |
|---|---|---|
| **#1 Windows／ブラウザdriver** | `windowId` によるWindows/Linuxアプリの指定、ブラウザTab用driver、追加roleへの対応 | 投稿者はWindowsブラウザでdry-runを報告。実操作の完走実証とは別。 |
| **#2 CRLF修正** | AXの行分割をCRLF対応にする | 投稿者は既存18件に回帰テストを追加し、19件通過と報告。 |
| **#3 実行直前の再観測** | 判断・パラメーター準備の後、AX全体を再取得して変化があれば実行せず戻す | 投稿者の検証はモックdriver。原子的な観測・操作を保証する修正ではない。 |

**#3は特に重要**。mainでは、観測した後にJevを待ち、その後で同じ要素番号を使って操作する。その待ち時間にUIが変わり、同じ番号が別のコントロールに割り当てられるケースがある。PRでは「Nextだった番号がDelete eventに変わる」ケースをモックで再現している。

修正は保守的で、AX全文に何らかの変化があれば止める方式。無関係な変化でも止まるし、再観測と実行の間の競合まではなくならない、と投稿者自身が説明している。

別のIssueでは、中国語の `按钮` や `文本` というroleが、英語限定の候補フィルターで落ちる問題も報告されている。**これはJevが中国語を理解できないという話ではなく、Jevに届く前の入力アダプターの問題**よ。今回の関数抜粋によるチェックでも、同種の入力から候補が0件になることを確認した。

---

## 推測（示唆）

### 1. 汎用化するなら、中心に置くべきは「候補要素」より「具体化済みの操作」

私なら、このプロジェクトから分業と注入可能な構造は借りる。でも、現在の

```text
対象を選ぶ ＋ 操作種類を選ぶ ＋ 後から引数を渡す
```

という境界は、そのまま持ち出さないわ。

理由は、ここまで見た通り、**判定時と実行時で操作内容が変わり得る**から。

代わりに、先に一つの操作提案へまとめる。

```ts
type ActionProposal =
  | {
      kind: "click";
      observationRevision: string;
      targetRef: string;
    }
  | {
      kind: "setValue";
      observationRevision: string;
      targetRef: string;
      value: string;
    };
```

実際には、対象の世代番号、実行前提、必要な権限なども加える。このオブジェクトをJevの判定、ローカルの検査、実行で共有し、**検査後に別の座標や文字列へ差し替えない**。

危険性や権限の分類も、Jevや画面内の文字列だけに自己申告させるのではなく、実行アダプター側が知っている操作仕様から付けるべきだと思う。

「Searchというラベルだから安全」と「この操作は検索欄の値だけを変更する」は、似ているようで全然違うのよ。

### 2. 四問独立の問題は、一括問い合わせを捨てなくても直せる

君の関心からすると、ここが一番おいしいところでしょう。

具体的な操作案を先に作れるなら、次のように投げられる。

```text
共通state：
  操作案A = この検索欄へ、この文字列を設定する
  操作案B = この検索ボタンを押す
  操作案C = 追加の状態を観測する

Jevへの一括質問：
  次に進めるべき操作案はどれか？
  操作案Aには確認が必要か？
  操作案Bには確認が必要か？
  操作案Cには確認が必要か？

コード：
  選ばれた操作案に対応する判定結果を参照する
```

こうすれば、それぞれの質問が独立でも問題ない。**何を審査するかが、回答を待つ前に確定している**から。

これは公式が説明する「必要になるかもしれない質問を先にまとめて評価し、コードで使う結果を選ぶ」方式と整合する。逆に、操作内容や追加の証拠が最初の回答を得るまで確定しないなら、その部分は二段階にする必要があるわ。([TypeSafe AI](https://docs.typesafe.ai/primitives))

もちろん、全要素×全操作を無条件に膨らませる必要はない。まずコードで不適合な組み合わせを落とせばいい。

**Jevに無効な操作を見抜かせる前に、無効な操作を作らない。** 分類器の賢さを節約するというより、契約の仕事を分類器へ押し付けない、ということね。

### 3. 抽出する価値があるのは、判断ループより「観測・実行・検証の契約」

私なら、再利用する中核はこう整理するわ。

```text
版付きの観測
  → 必要な証拠と操作案を構築
  → Jevで適切さ・曖昧さ・追加確認の必要性を判定
  → コードで権限・引数・対象・前提を検証
  → 実行直前に対象の鮮度を確認
  → 同じ操作案を実行
  → 事後条件と進捗を検証
```

この構造なら、Jevは判断器として交換できる。一方で、対象の同一性、入力値、権限、状態の版といった約束は、判断器を交換しても維持できる。

PR #3の全文比較は、その最初の安全策としては理解できる。ただ、汎用化するなら、画面中の無関係な時計表示まで同一であることを求めるより、**選んだ対象と、その操作の前提に関係する状態が変わっていないか**を確認する方向が欲しい。

さらに、ゲームや自分で管理するアプリなら、実行APIへ `expectedRevision` を渡して、不一致時に操作を拒否できる。外部GUIより、こうした契約を強く作れる余地があるわ。

### 4. RuneweaveとWardenへの示唆は、少し違う

**Runeweaveでは、Jevを「完成済みの合法行動から選ぶだけ」に閉じ込める必要はない**と思う。前の「AXが合法Action APIに置き換わるだけ」という説明は、単純化しすぎだったわ。

観測範囲を広げる、対象の詳細を見る、変化を追う、追加情報が必要かを判断する。そのような操作も同じ意味層に載せられる。Jev-cuから借りるべきなのは要素リストそのものではなく、**構造化した観測を使って小さな判断を繰り返し、実際の状態で検証する**という流れね。

**Wardenでは、今回の実行境界のずれが直接の教訓になる。**

ツール名や説明文が無害そうかを判定するだけでは足りない。実際に渡すコマンド、対象パス、書き込み先、操作引数を一つの要求として確定し、その要求を検査して、その要求を実行する必要がある。

根拠は同じよ。**「検査した要求」と「実行した要求」が違えば、分類精度がいくら高くても、その精度は実行の安全性を支えていない。**

### 5. 次に評価するなら、速度より先に「無介入で検証済みの成功」を測る

私なら、今後の評価軸は単なる要素選択精度から変えるわ。

中心に置くのは、**人やPlannerの接管なしに、事後条件で成功を確認できた割合**。その上で、接管理由、誤った完了判定、無効操作の繰り返し、成功1件あたりの総時間を見る。

現在のログには判定・ゲート・操作・画面変化などが残るけれど、ステップ記録の候補は件数で、実際の操作引数や完全な観測状態を揃えて保存するものではない。改善のための再現基盤にするなら、プライバシーに配慮しながら、入力状態の版と実行した操作案を追える記録が必要だと思う。

---

**私の評価は、「完成品としてそのまま採用する」より、「設計パターンと失敗例をまとめて学べる、小さくて読みやすい実装」ね。**

魅力は本物よ。大きなLLMを毎クリック起こす代わりに、構造化した状態から安い判断を連続させる。その分業は筋が通っている。

ただし、そこで厚く作るべきなのはJevのプロンプトではなく、**観測したもの・判定したもの・実行するものを一致させる契約**。ここを固めると、君が考えている制御層の部品として、ぐっと価値が出るわ。

同じ最小チェックを手元のチェックアウトに対して行う[再現用スクリプト](sandbox:/mnt/data/jev-cu-review/jev-cu-review-probes.mjs)も置いた。今回実行したのは関数抜粋版で、既存テスト全体や実機CUAの再実行とは区別してある。

Windows対応・CRLF修正・実行前再検証の3件がマージされたら通知する監視も設定しておく？

---

# you asked

message time: 2026-09-20 07:50:31

筋よく自前実装する構想をしてみよう

---

# chatgpt response

いいわ。自前で作るなら、**「Jevが次のクリックを選ぶツール」ではなく、「必要な観測を集め、短い操作計画を実行し、結果を確かめるランタイム」**にしたい。

ただし、安全性の話だけ大きくして、結局すべてで人に聞くものにはしたくないのよ。狙うのは、**普段は軽快に進み、分からなくなったときには“何が分からないのか”を切り分けて復旧できること**。

まずは独立した実装として構想するわ。特定のエージェント環境にも、特定のOSにも、Jevそのものにも中核を結び付けない。ただ、最初から全部作ることもしない。

## 事実 — 設計に効く前提

Jevは同じstateに対して複数の質問を独立に評価する。したがって、**既に具体化されている操作案の比較・審査は一括化できるが、同じリクエストの別回答を前提にした判断はできない**。また、現在の公式仕様では入力はテキストで、画像は別の認識経路が必要よ。([TypeSafe AI](https://docs.typesafe.ai/))

公式の `jev-1.13` の注意事項では、数値計算・日時比較・多段の間接参照・無関係な情報の多いstateなどが弱点として挙げられている。`confidence` も回答分布から計算した統計量であって、その操作の成功率そのものではない。**意味判断を任せることと、数値処理や整合性検証まで任せることは分ける必要がある**わ。([TypeSafe AI](https://docs.typesafe.ai/model-jaggedness/jev-1.13))

操作基盤については、WindowsのUI Automationには操作能力を表すControl Patternsがあり、macOSにもアクセシビリティオブジェクトへの操作APIがある。ただし、UI要素の識別子を永続IDとして扱うのは危険で、たとえばWindowsのRuntimeIdは時間の経過で再利用され得ると明記されている。([Microsoft Learn](https://learn.microsoft.com/en-us/windows/win32/winauto/uiauto-controlpatternsoverview))

この前提から、以下を設計案として組み立てるわ。まだ実装・実機検証したものではない。

## 推測（示唆）— 自前実装の構想

### 1. 中心に置くのは「クリック」ではなく「小目標を達成する実行単位」

入口は、たとえばこういう要求にする。

> このアプリで新しい文書を作り、指定した本文を入力して、入力内容が一致するところまで進める。保存や共有はまだしない。

呼び出し元が渡すのは、細かなマウス操作ではなく、**小目標・入力データ・成功条件・許可範囲**。

中では次のループを回す。

```text
小目標を受け取る
    ↓
現在の状態と、不足している情報を整理
    ↓
必要なら追加観測
    ↓
具体的な操作案を作る
    ↓
意味的な適合性を評価 ＋ コードで契約を検査
    ↓
実行
    ↓
結果を観測して、小目標の達成／継続／復旧を決める
```

分業はこうね。

| 担当 | 主な仕事 |
|---|---|
| 生成モデル | 小目標への分解、自由な文章生成、未知の状況への仮説、新しい手順の提案 |
| Jev | 状況の意味付け、注意を向ける場所、対象の対応付け、情報の十分性、進捗や異常の判断 |
| 通常のコード | 状態管理、質問の組み立て、引数確定、認可、実行、数値処理、検証、予算と再試行 |
| OS・アプリ用アダプター | 観測と操作能力の公開、実行直前の対象確認、実際の操作、結果の取得 |

ここで大事なのは、**生成モデルを「困ったときの非常口」に限定しないこと**。

新しい入力文を作る、目標を別の切り口で分解する、今までなかった仮説を出す。それは普通の仕事として呼ぶ。ただし、同じ画面の同じ入力欄を毎回説明し直すような使い方は避ける。

「LLMを呼ばない」が目的ではなく、**LLMを呼ぶたびに、ちゃんと新しい仕事をしてもらう**設計よ。

### 2. 観測は「要素一覧」ではなく、問い合わせできる意味モデルにする

AXツリーを文字列へ潰して、上位40件を渡すところからは離れたい。

内部の正本は、最初なら普通のJSONで十分。グラフデータベースなどは要らないわ。

```text
Observation
  対象アプリ・ウィンドウ・セッションの世代
  取得時刻・観測ID
  ノードと親子関係
  ラベル・値・状態・利用可能な操作
  フォーカス・モーダルなどの関係
  取得できた範囲／省略した範囲
  情報源
```

特に、次の区別を失わないこと。

**観測されたこと**は「この入力欄の現在値は空文字列」。

**コードで導いたこと**は「必要な三項目のうち二項目は入力済み」。

**モデルが推測したこと**は「この欄は請求先のメールアドレスだと思われる」。

この三つを同じ `state` に雑に混ぜない。推測には根拠の参照と、どの観測に基づいたかを付ける。

そして、全体を毎回渡す代わりに、目的別の観測窓を作る。

> 現在のダイアログの入力欄と、そのラベル・説明・エラー表示を取得する。

> このボタンの親領域と、同じ名前の別ボタンを取得する。

> 前の操作から変わった値と、現在のフォーカスを取得する。

私はこれを、内部では **Lens――観測の切り口**として扱いたいわ。

「どれを押す？」の前に、「判断するためにどこを見る？」がある。ここをJevへ渡せるようにするのね。

ただし、**スクロールやメニュー展開は、観測目的でも副作用のある操作**。単なる読取APIに偽装せず、通常の操作経路を通す。

#### 省略したことも、データとして渡す

たとえば候補を絞るなら、

```text
対象領域：請求先フォーム
取得済み：入力欄8件、説明文3件
省略：別タブの内容、折り畳まれた住所欄
取得状態：この領域内では完全
```

という情報を残す。

これがないと、「存在しない」と「今回は渡されていない」が区別できないでしょう。**候補に見つからないことを、世界に存在しないことへ変換させない**のよ。

### 3. Jevには「意味的な対応付け」と「次の観測」を厚く担当させる

ここは、単なる操作選択器より一段広くする。

たとえばフォーム入力では、いきなり「次に何をする？」と問わなくていい。

ユーザーが指定した情報は、

```text
氏名
連絡先メール
請求先住所
```

画面側には、ラベルや所属領域を持つ複数の入力欄がある。

そこでJevには、**必要な情報とUI要素の対応関係**を評価させる。

```text
この欄は「連絡先メール」に対応するか？
この欄は「請求先住所」に対応するか？
同名の欄を区別するための情報が足りているか？
この説明文は、どの欄の制約か？
```

コード側で重複割当や必須項目の欠落を検査し、曖昧なところだけ追加観測する。

これは「次の操作を毎回一から選ぶ」より、**やるべきことを画面へ結び付ける**使い方ね。入力値そのものはユーザーの指定や生成モデルから受け取り、日時の変換や桁数検査などはコードで処理する。

Jev用の質問群は、大きく次の五系統でよさそう。

| 系統 | 質問の例 |
|---|---|
| 状況 | 目的の画面か。別のダイアログに遮られているか |
| 対応付け | この要素は、目標中のどの対象・項目に対応するか |
| 観測の十分性 | 対象を区別できるか。どの追加情報が必要か |
| 操作案 | この具体的な操作は、現在の小目標に寄与するか |
| 結果・復旧 | 期待した変化が起きたか。待機・再観測・別手段のどれが必要か |

そして必ず、**「該当なし」「情報不足」「別の仮説が必要」**を表せるようにする。

有限の質問へ落とすことと、探索全体を最初の候補集合に閉じ込めることは別よ。質問の外側で、観測範囲も仮説も小目標も更新できればいい。

### 4. 質問を「依存関係ごと」にまとめる

Jevの一括評価は積極的に使いたい。ただし、「とにかく一回に全部」ではない。

**同じ情報だけで答えられる質問は、まとめる。新しい情報が必要な質問は、次へ送る。**

たとえば、操作案が既に確定しているなら、

```text
操作案A：
  ダイアログDの入力欄Eへ、文字列Vを設定する

一括で問う：
  Aは現在の目標に合っているか？
  Eは目的の入力欄か？
  周辺の説明とVの内容は整合するか？
  Aについて追加確認が必要だと示す情報はあるか？
```

という形にできる。

一方、

```text
メニューを開く
    ↓
新しく出てきた項目を観測する
    ↓
項目を判断する
```

は、原則として分ける。まだ見ていないメニュー内容を、現在の画面だけで審査済みにしない。

公式のspeculative fan-outも、必要になるかもしれない質問を先に評価し、後からコードで使う結果を選ぶ方式よ。**先に質問できるのは、質問に必要な材料が既にある場合**、という線引きで使う。([TypeSafe AI](https://docs.typesafe.ai/patterns/fan-out))

実装は、最初から大きなワークフローエンジンにしなくていいわ。

`buildQuestions(frame)` が、必要な証拠の参照と質問IDを返す普通のTypeScript関数で始める。依存する証拠が欠けていれば、問い合わせではなく追加観測を返す。

また、同じモデルへ似た質問を大量に投げても、それを独立した証人の多数決としては扱わない。**一括質問の数と、証拠の独立性は別**だから。

### 5. 操作は「具体化済みの一件」として確定させる

前のレビューから持ち帰るべき一番重要なところね。

**対象を判定した後に、別の座標や別の入力値を差し込める経路を作らない。**

操作の型は、まず小さくていい。

```ts
type Command =
  | {
      kind: "invoke";
      targetRef: string;
    }
  | {
      kind: "setValue";
      targetRef: string;
      value: string;
    }
  | {
      kind: "setChecked";
      targetRef: string;
      checked: boolean;
    };

interface OperationProposal {
  schemaVersion: 1;
  taskId: string;
  observationId: string;
  command: Command;
}
```

これは契約の形を示す例よ。実際の実行経路では、さらに操作ホストが対象を解決し、前提条件と実行可能性を検査する。

```text
操作案を作る
  → 操作ホストが対象・引数を具体化
  → 確定内容を保持し、参照IDを返す
  → その内容に対して意味判断・認可
  → 同じ参照IDの操作だけを実行
```

TypeScriptの `readonly` や内容ハッシュだけを、安全境界にはしない。**実行側が保持する内容と、そこで検査する権限が正本**。

内容ハッシュは取り違え防止には使えるけれど、ハッシュが一致すること自体は、実行してよい理由にならないわ。

#### 座標操作は別の能力にする

`invoke` が失敗したから、自動的に座標クリックへ落とす――という隠れたフォールバックは避ける。

座標クリックなら、最初から座標クリックの操作案として、対象ウィンドウ、座標系、根拠となる画像、画面配置の版を持たせる。

それでも視覚認識の誤りや競合は残る。だから、構造化された要素操作と同じ保証だとは扱わない。

#### 操作能力と、業務上の効果を混同しない

ここは特に慎重にしたい。

UIAやAXが「このボタンは実行できる」と教えてくれても、**それだけで“押すと検索だけが起こる”“外部送信しない”と分かるわけではない**。

自前アプリや検証済みのアプリ連携では、操作の意味を契約として持たせられる。汎用GUIでは分からない部分を `unknown` のまま残し、許可方針を保守的にする。

ラベルの意味判定は補助情報。権限の発行元にはしないわ。

### 6. ランタイムが持つべきなのは「停止条件」より「復旧の状態機械」

単なる `for (step...)` ではなく、次の区別を持たせたい。

```text
観測中 → 判断中 → 操作準備 → 実行中 → 結果確認
                    ↓                    ↓
                  再観測            継続／復旧／完了
```

特に重要なのは、**「失敗した」と「実行されたか分からない」を分けること**。

| 状況 | 扱い |
|---|---|
| 対象が消えた、対象の意味が変わった | 実行せず再観測する |
| 必要な操作能力がない | 別の操作案を作るか、生成モデルへ戻す |
| 実行要求は返ったが、期待した変化がまだない | 期限付きで結果を待つ |
| タイムアウトして、実行されたか不明 | 再送せず、まず事後状態を調べる |
| 同じ失敗を繰り返している | 同じ操作を打ち切り、観測・手順を変える |
| 人間が操作に介入した | 未実行の操作を失効させ、制御を譲る |

「送信を要求したらタイムアウトした。ではもう一度送信」は避けたいでしょう。

操作IDでランタイム内部の重複要求は抑えられても、汎用GUIの副作用を完全なexactly-onceにはできない。**結果不明を正規の状態にする**方が筋がいい。

また、実行直前の再検査は、画面全体の文字列一致ではなく、対象と関連する前提に寄せる。

対象が同じか。目的のウィンドウか。必要なダイアログがまだあるか。入力先は編集可能か。

ただし、外部アプリのGUIでは、この確認と実行を原子的にできるとは限らない。自前アプリなら `expectedRevision` を操作APIで検査する強い契約を作れるけれど、外部GUIは残る競合を前提にするわ。

#### 完了は、操作の成功とは別にする

最終結果も、最低限この三つを分けたい。

```text
verified_success
    成功条件を証拠で確認できた

completed_unverified
    操作経路は終わったが、成功条件の確認が足りない

blocked / failed / outcome_unknown
    停止・失敗・結果不明
```

ボタンを押せたことと、目標を達成したことは違う。

たとえば本文入力なら、**対象文書から読み直した本文が一致するか**を確認する。確認できない項目が残れば、それを明示して終える。

### 7. 速さは「モデルを小さくする」だけでなく、「判断し直さなくてよい部分」を増やして作る

ここは自前実装の面白いところよ。

毎操作で必ずJevを呼ぶ必要はない。一度、対象との対応付けが確かめられた短い手順は、条件付きでコードが走らせればいい。

たとえばフォームなら、

```text
Jev：
  氏名・メール・住所と、各入力欄の対応をまとめて評価

コード：
  氏名を設定 → 値を検証
  メールを設定 → 値を検証
  住所を設定 → 値を検証

途中で構造や前提が変わったら：
  手順を止めて再観測
```

**逐一の検証は残すが、逐一の意味判断は省ける**。

この短い手順を、最初は再利用可能なレシピとして表現する。

レシピが持つのは、対象を探す条件、引数の型、実行前提、各段階の検証、想定外への出口。固定の要素番号や画面座標を保存するマクロとは分けるわ。

ブラウザのPlaywrightは、操作のたびにlocatorから現在のDOM要素を見つけ直し、クリック時には可視性や操作可能性などを検査する。この考え方は参考になる。ただし、再検索できたことと、業務上同じ対象であることは別なので、自前側でも対象の意味と前提を確認する。([Playwright](https://playwright.dev/docs/locators))

さらに、観測イベントをそのまま全部Jevへ送らない。

時計表示だけ変わったなら、入力欄の対応付けを評価し直す必要はない。一方、新しいモーダルが出たら、その手順を中断する。

最初は高度なキャッシュより、**「何の変化で、その判断を無効にするか」**を明示する方を優先したい。

モデル応答が返るまでに関連状態が変わっていたら、その応答はログに残しても、現在の操作許可には使わない。

### 8. 技術構成は、TypeScriptのコア＋小さなネイティブ操作ホスト

私は最初をこうするわ。

**制御コアはTypeScript／Node.js。Mac用操作ホストはSwift。別プロセス間を、版付きのJSONメッセージでつなぐ。**

理由は、質問組み立て・状態機械・検証・トレースを修正しやすくしつつ、OSの操作APIを無理に同じ言語へ押し込まないためよ。

責務を分ければ、最初のディレクトリもこの程度で足りる。

```text
core/         タスク、状態機械、操作契約、検証
semantic/     観測窓、質問生成、Jev provider
adapters/     テスト環境、OS・アプリとの接続
hosts/macos/  Swift製の観測・操作ホスト
cli/          起動、状態確認、中断、トレース再生
tests/        契約テスト、障害注入、受入シナリオ
```

初期のプロセス間通信はstdioで十分。ネットワーク待受、リモート認証、サービス常駐まで一度に持ち込まない。

後でMacからWindowsを操作する場合も、Windows側にはネイティブの操作ホストを置く構成にする。制御ロジックがどこで動くかと、実際のデスクトップへアクセスするプロセスは分離しておく。

OS別の方針はこうね。

| 対象 | 採りたい経路 |
|---|---|
| macOS | AXによる観測・要素操作を最初の実機経路にする。Swiftから公式APIを扱う。([Apple Developer](https://developer.apple.com/documentation/applicationservices/axuielement_h?utm_source=chatgpt.com)) |
| Windows | UI AutomationのControl Patternsを公開するホストを追加する。言語はC#を第一候補にする。([Microsoft Learn](https://learn.microsoft.com/en-us/windows/win32/winauto/uiauto-controlpatternsoverview)) |
| ブラウザ | Playwright用アダプター。OS側AXでブラウザ全体を無理に操作する経路と分ける。([Playwright](https://playwright.dev/docs/locators)) |
| Linux | 意味観測・対応操作はAT-SPIを軸にし、必要な画面取得や入力制御はportal系と分けて検討する。([Gnome Pages](https://gnome.pages.gitlab.gnome.org/at-spi2-core/libatspi/)) |

LinuxのRemoteDesktop portalにもユーザーによる共有・デバイス許可のセッションがある。ここは独自の万能権限で迂回するより、基盤の許可モデルに合わせて設計したい。([Flatpak](https://flatpak.github.io/xdg-desktop-portal/docs/doc-org.freedesktop.portal.RemoteDesktop.html))

ただし、**最初に実装するネイティブホストはMacの一つだけ**。WindowsとLinuxは、契約の設計時に考慮するが、同時に完成させようとはしない。

### 9. 安全境界は三つに分ける。ただし、万能なサンドボックスとは呼ばない

権限を「このアプリを触ってよい」だけにまとめるのは弱い。

**観測の許可**、**モデルへ送信する許可**、**操作する許可**を別々にするわ。

ローカルで見られるからといって、そのまま外部モデルへ送ってよいわけではない。パスワードなどの機密値は、モデルに値そのものを見せず、操作ホスト内の参照で扱える余地を残す。

また、モード名は挙動を明確にする。

`replay` は保存済みの観測だけで動く。

`observe` は実環境を読むが操作しない。

`preview` は操作案まで作るが実行しない。ただしモデル問い合わせの有無は別設定。

`execute` は許可範囲内で実行する。

**dry-runだから外部送信もない、という曖昧さをなくす**のね。

それでも、汎用GUI越しに操作するアプリの全副作用を、このランタイムだけで封じ込められるわけではない。厳密な隔離が必要なら、テスト用アカウントや隔離環境など、OS側の境界が別途必要になる。

同様に、呼び出し元へ別の生操作ツールを残したまま、「全操作がこのランタイムで保護される」とは言わない。

**制御できる範囲を明確にして、その範囲では抜け道を作らない**という方針よ。

### 10. 自己改善は「判断とレシピ」を育てる。権限や合格条件は勝手に緩めない

これは後から付ける装飾ではなく、最初から記録だけ用意しておきたい。

保存するのは、操作ログだけではない。

どの小目標に対して、何を観測し、何を省略し、どの質問を投げ、どの操作を確定し、どの証拠で成功・失敗を判定したか。その一連を追えるようにする。

そこから改善ループを回す。

```text
失敗した実行
  → 原因を分類
  → 観測窓・質問・レシピの変更案を生成
  → 固定テストと対照ケースで評価
  → 良い変更だけ採用
```

たとえば、「対象選択に失敗した」という一種類のエラーにせず、

> 必要な要素を観測していなかった。

> 要素は見えていたが、所属フォームを落としていた。

> 対象判断は正しかったが、操作直前に入れ替わった。

> 操作は成功していたが、結果確認が早すぎた。

と分ける。

原因によって直す場所が違うからよ。モデルの質問文だけを延々といじる、あの不毛な儀式を避けたい。

改善対象は、観測の選び方、意味判断の質問、対象の探索条件、短い手順。**許可範囲や成功条件を、失敗したから自動的に緩めることはしない**。

それから、録画済みのトレースだけでは、別の操作を選んだ世界は評価できない。再生テストと、実際に分岐できるテスト環境を両方持つ必要があるわ。

### 11. 最初の実装は、四段階で十分

**P0：モデルなしで契約を通す。**

小さなテスト用UIを作り、観測、要素参照、操作確定、実行、結果確認までつなぐ。応答は固定でいい。

ここで、対象の入れ替わり、同名ボタン、遅延、フォーカス変更、実行後の応答喪失を注入できるようにする。

最初の合格条件は、「賢く操作する」ではなく、**間違った前提では実行せず、実行結果が不明なときに二重実行しないこと**。

**P1：Jevで対応付けと追加観測を回す。**

テスト用フォームで、複数項目を入力し、確認画面で止まるところまで動かす。

ラベルの言い換え、同名項目、折り畳まれた欄、検証エラーを加える。日本語UIも最初から評価対象に含める。公式にはCJKでの精度上の制約が説明されているので、英語だけ通して対応済みとはしない。([TypeSafe AI](https://docs.typesafe.ai/concepts/state))

ここで測る中心は、**生成モデルの介入なしで、検証済みの成功に到達した割合**。

**P2：Macの実アプリへ接続する。**

まずはTextEditなどで、対象文書への定型文入力と本文一致の確認という狭いタスクを通す。

テストUIの都合のよい契約が、現実のAXにどれだけ載るかをここで確かめる。保存、ファイル選択、複数アプリの横断は、その後に増やす。

**P3：短いレシピの再利用と、生成モデルからの呼び出しを加える。**

呼び出し元へ、タスク投入、状態取得、中断、追加情報の供給を公開する。SkillやMCPはこの外側の薄い入口にする。

中核をSkill文書の指示に依存させず、停止・再試行・失効・検証はコードの仕事にするわ。

評価では、成功率だけでなく、**誤った成功報告、結果不明、再計画回数、人間への割り込み、成功一件あたりの総時間**を分ける。止まりまくる実装を「安全で高精度」と評価してしまわないためね。

---

## RuneweaveやWardenとは、どこを共有するか

Runeweave側の既存案には、意味付きグラフを正本にし、ラベルを操作参照に使わず、観測と推測を分離する方針がある。この部分はかなり素直に接続できるわ。

ただし、共通化するのは「すべての対象をGUIボタン扱いすること」ではない。

共有したいのは、**版付きの観測、追加観測の要求、意味判断、操作契約、結果の証拠、復旧の状態**。

Runeweaveならゲーム内の意味と操作をそのまま公開する。外部デスクトップならAXやUIAから組み立てる。自前アプリの方が強い契約を持てるなら、その強さをわざわざGUIの最小公倍数まで落とさない。

Wardenとの共有も、最初から巨大な統合基盤にするより、質問・証拠・判断記録の形式を合わせる程度から始めたい。**実際に二つの環境で同じものが必要になってから、共通部分を抽出する**方が堅いでしょう。

---

私なら、この構想の中心をこう置くわ。

> **開かれた目標を、観測で裏付けられた短い実行契約へ変換する。Jevは、その変換を細かく、速く回すための意味判断器。**

最初に作るべきなのは、豪華なエージェントUIでも、万能なプランナーでもない。

**「意味モデル → 具体的な操作 → 実行結果の証拠」が一本につながり、途中で状況が変わっても壊れず戻れる、小さな縦切り**よ。そこへJevを入れて、追加観測と対応付けを賢くする。

その順番なら、Jev-cuの発想を活かしながら、単なる改良版ではなく、君が考えている“広い意味の制御層”へ育てられると思うわ。

---

# you asked

message time: 2026-09-20 08:03:53

名前を考えよう

---

# chatgpt response

そうね。今回はかなり名前を付けやすい概念よ。単なるComputer UseでもAgentでもなく、**「意図を観測可能な操作へ落とし、実行結果まで証明する制御層」**だから、そこを名前にしたい。

私はこの辺が好き。

- **Praxis** — 思想・意図を「行為」に変える、という意味がほぼ直球。今回の本質にかなり合う。ただし一般語として強すぎて検索性は弱そう。
- **Telos** — 「目的・終着点」。goal-drivenなランタイム感がある。操作より目的側に重心がある名前。
- **Ariadne** — 糸を辿って迷宮から抜ける。観測→判断→操作→検証→復旧というループとの相性が妙にいい。GUIという迷宮を歩く感じもある。
- **Gnomon** — 観測から状態を読み取る器具、という比喩が綺麗。かなり渋い。「行動」より「観測と意味づけ」寄り。
- **Latch** — 状態を確認し、操作を確定してから実行する。短くてCLI名としても強い。今回の「検査した操作と実行する操作を一致させる」という思想にも合う。
- **Witness** — 「実行した」ではなく「結果を証拠で確認する」という設計思想を真正面から出せる。かなり特徴的。
- **Relay** — Planner/Jev/OSの間をつなぐ制御層。分かりやすいが少し一般的。
- **Verge** — 意図と実行、観測と行為の「境界」。響きはかなり好き。ただ、何をするものかは名前からは分からない。
- **Trellis** — 行動が伸びていくための構造・足場。Lattice系の感触もあるが、既存の `lattice` と近すぎるかもしれない。
- **Noema** — 「知覚された意味」。AX/DOM/UIAの生情報を意味モデルへ変換する部分には非常に合う。ただし実行ランタイム全体の名前としては知覚寄り。
- **Praetor** — 判断・命令を出す制御者。格好いいが、ちょっと「司令塔」感が強すぎる。
- **Wayfinder** — 観測して、次にどこへ進むか決める。意味が非常に伝わりやすい。少しプロダクト名っぽい。
- **Threadmark** — 造語寄り。「辿っている実行経路」と「状態に付ける印」。今回のtraceabilityをかなり表現できる。
- **Proofstep** — 「一歩ごとに証拠を持つ」。技術名としては説明力が高い。ただし風情は薄い。
- **Vellum** — 観測・操作・証拠が記録される台帳のニュアンス。ランタイムよりtrace基盤っぽい。

今の構想まで含めて、私は **Ariadne / Latch / Witness** の三つを推すわ。

**Ariadne** はプロジェクト全体の思想を一番よく表している。

```text
Goal
  ↓
observe
  ↓
interpret
  ↓
act
  ↓
verify
  ↓
if lost, follow the thread back
```

「迷路の中で賢くクリックする」ではなく、**糸を切らずに進む**。観測ID、操作契約、trace、復旧まで全部「糸」の比喩で語れるのが強い。

**Latch** は実装思想として一番鋭い。

```text
observe
→ bind
→ validate
→ latch
→ execute
```

「この観測に対する、この操作」を一度ラッチして、そのまま実行する。Jev-cuで見えた最大の問題への回答がそのまま名前になっている。CLIも、

```bash
latch run task.yaml
latch inspect
latch replay trace.jsonl
```

かなり自然よ。

そして **Witness** は、他のAgent系と最も違う部分を名前にしている。

> 成功したと思うのではなく、成功したことを観測する。

```bash
witness run
witness trace
witness verify
```

これも相当いい。

ただ、一つ捻るなら、私は **Ariadne** が今の君のプロジェクト群には一番似合うと思う。`Runeweave` と並べても、

> Runeweave — 意味のあるゲーム世界  
> Ariadne — 意味のある世界を辿り、操作する実行系

という関係が妙に綺麗なのよ。

内部用語まで統一するなら、

```text
Ariadne
├─ Thread      実行トレース
├─ Lens        観測窓
├─ Knot        確定した操作契約
├─ Witness     実行後の証拠
└─ Waypoint    小目標
```

みたいにできる。

これはかなり強い命名体系だと思うわ。特に **`Knot` = 観測に結び付けられて変更不能になった操作案** は、設計概念としてもいい。ちょっと出来すぎていて腹立つくらいね。

---

# you asked

message time: 2026-09-20 08:04:53

Ariadne、ariaともかかってていいね

---

# chatgpt response

そうなのよ。しかも思った以上に多重にかかってる。

**Ariadne** の先頭がそのまま **ARIA** で、今回の土台にするアクセシビリティ意味情報とも直結する。WAI-ARIA自体が、UIの **role / state / property / relationship** を支援技術へ伝えるための仕組みでしょう。今回の「画面をピクセルではなく意味として読む」という思想と相性が良すぎる。([W3C](https://www.w3.org/WAI/standards-guidelines/aria/?utm_source=chatgpt.com))

さらに、

- **Ariadne** — 迷宮を糸で抜ける
- **ARIA** — UIの意味構造
- **aria** — 独唱曲
- **orchestration** — 複数の判断器・実行器を指揮する

まで自然につながる。

`境界のオルケストラ` 周辺とは別物なのに、音楽語彙が勝手に再出現してるのもちょっと面白いわ。

内部概念もかなり綺麗に揃えられそう。

```text
Ariadne

Thread
  └─ 一連の実行履歴

Lens
  └─ 現在見るべき観測範囲

Knot
  └─ 観測に結び付けて確定した操作

Waypoint
  └─ 小目標

Witness
  └─ 操作後の証拠

Score
  └─ 実行計画 / レシピ

Aria
  └─ 一つの意味的な自律実行単位？
```

特に最後が悩ましくて面白い。

**Ariadne本体の内部で `Aria` を第一級概念にしてしまう**のもありだと思う。

たとえば、

```text
Aria = goal + observation strategy + actions + verification
```

つまり、

> 「メールアドレス欄を特定して値を設定し、反映を検証する」

くらいの、**短く閉じた意味的実行単位**を Aria と呼ぶ。

そして、

```text
Task
 └─ Waypoint
     ├─ Aria
     ├─ Aria
     └─ Aria

Thread = 実際に演奏された履歴
Score  = 再利用可能なAriaの構成
```

になる。

これは単なる洒落以上に、設計用語としてかなり使えるわ。

`aria run` というCLIにすると流石に本体名との関係が分かりづらいけど、

```bash
ariadne run
ariadne inspect
ariadne replay
ariadne score test
```

で、内部ログに

```text
aria.started
aria.observed
aria.bound
aria.executed
aria.verified
```

が流れるのはかなり好き。

私はもう **Ariadne** でかなり固まってきたわ。  
偶然にしては意味が重なりすぎてる。こういう名前は後から説明を足さなくても、設計が育つほど勝手に意味が増えるタイプよ。

---

# you asked

message time: 2026-09-20 08:05:29

現時点で詰められることを詰めていこう

---

# chatgpt response

Ariadneを実装へ渡せる粒度まで固めるわ。まず実行契約・復旧ルール・初版の受入条件を詰め、実機検証が必要な部分を切り分ける。

初版APIはTask・Observation・Operation・Evidenceへ絞り、比喩語彙は内部概念に限定するわ。観測はロックではない前提で競合を扱い、Jevの意味判断と権限判定も分離する。

---

# chatgpt response

いいわ。ここからは、**初版で何を受け取り、どこまで自律的に進み、何を根拠に止まるか**を固定していこう。

特に、前の構想から一歩進めたいのはここね。

> **Ariadneの基本動作を「毎回Jevに次の操作を聞く」にはしない。  
> 意味を判断して対象を対応付け、成立している前提の下ではコードが短い手順を実行する。**

意味判断は必要なところへ集中させる。その代わり、前提が変わったこと、検証できなかったことを曖昧にしない。

実装へ引き継げるように、[設計書](sandbox:/mnt/data/ariadne-design-v0.1/README.md)と、JSON契約・例・検査コードを含む[設計一式](sandbox:/mnt/data/ariadne-design-v0.1.zip)も作ったわ。以下が、その中核になる。

## 事実 — 今回の仕様に効く前提

Jevの同一リクエスト内の質問は、同じstateに対して独立に評価される。また、**質問IDはモデルに渡されない**。`bind_contact_email` と名付けただけでは「連絡先メール欄を探す」という指示にならず、質問本文に対象と条件を書く必要がある。([TypeSafe AI](https://docs.typesafe.ai/primitives))

`confidence` は回答分布を要約した統計量で、特定のGUI操作が成功する確率そのものではない。公式も、閾値は用途と実際の評価に合わせて設定するものとしている。([TypeSafe AI](https://docs.typesafe.ai/confidence))

さらに、2026年9月17日確認のJev 1.13向け注意事項には、数値精度、多段参照、無関係な情報の多いstate、敵対的な内容による回答誘導が挙げられている。**型付き出力であっても、意味判断の誤りや入力からの誘導は残る**わ。([TypeSafe AI](https://docs.typesafe.ai/model-jaggedness/jev-1.13))

macOSのアクセシビリティAPIは、属性・アクション・通知・要素階層を扱う。一方、ボタンを押した後にアプリが何をするかはアプリ側の責務で、**「押せる」と「押して起こる効果が分かる」は別**なのね。([Apple Developer](https://developer.apple.com/library/archive/documentation/Accessibility/Conceptual/AccessibilityMacOSX/OSXAXmodel.html))

以下は、これらを踏まえた**Ariadneの設計案**。ランタイムやMacホストが既に動いているという話ではないわ。

## 推測（示唆）— Ariadne v0.1の設計を固める

### 1. 最初のユースケースは「意味付きフォーム入力」にする

初版で通したいのは、こういうタスクよ。

> 連絡先フォームに、指定された氏名とメールアドレスを入力する。入力先と反映内容を確認し、送信せずに終える。

画面には、たとえば同じ名前の欄が二つある。

```text
配送通知先
  └─ メール

連絡先
  └─ メール
```

単純なラベル検索では区別できない。一方、画面全体について自由に長考する必要もない。**名前・所属・説明を使った意味的な対応付け**に、Jevを使う余地がある。

そして、対象が特定できた後は、

```text
氏名欄へ設定 → 読み戻して確認
メール欄へ設定 → 読み戻して確認
最後に必須条件をまとめて確認
```

をコードで回す。

この題材なら、対象の曖昧さ、追加観測、複数操作、途中の画面変化、結果確認まで試せる。単に電卓のボタンを押すデモより、Ariadneの設計をきちんと試せるでしょう。

初版の範囲は、次のように絞る。

| 項目 | 採用案 |
|---|---|
| 実行範囲 | 一つのアプリ・ウィンドウ、一つの実行中タスク |
| 同時実行 | 副作用を伴う操作は一件ずつ |
| 最初の操作 | 要素への値設定、明示的に許可した要素の実行 |
| 意味判断 | 対象との対応付け、情報不足、追加観測、状況変化 |
| 生成モデル | 外側で目標分解や不足情報を準備する。内蔵の万能Plannerは作らない |
| 初版から除外 | 任意座標、任意キー、クリップボード、シェル、ログイン、複数アプリ横断 |

**操作を絞る代わりに、その操作を選ぶための観測は柔軟にする**。最初から何でもクリックできるものより、この方向が筋がいいと思うわ。

### 2. 公開APIの概念は増やしすぎない

名前の話から少し引き締めると、`Task → Waypoint → Aria` の階層は初版には入れない。

Ariadneという名前は残すけれど、APIまで神話事典にする必要はないのよ。

| 概念 | 意味 |
|---|---|
| `Task` | 目標、入力、要求範囲、固定した成功条件 |
| `Observation` | どこから、いつ、何を取得したか |
| `Binding` | 目標中の対象と、観測された要素との対応付け |
| `Operation` | 対象と引数が具体化された一件の操作 |
| `Evidence` | 判断や検証に使った、参照可能な根拠 |
| `Result` | 完了、停止、失敗、結果不明と、その根拠 |

`Lens` は内部で「観測の切り口」を説明する名前として使ってもいい。ただし、まずコード上は `ObservationQuery` で十分ね。

特に第一級にしたいのは **Binding** よ。

```text
Task側：
  「連絡先メールアドレス」

Observation側：
  node-email
  role = text_field
  name = メール
  parent = 連絡先

Binding：
  contactEmail → node-email
```

操作を直接選ぶ前に、この対応関係を作る。これが、意味判断と決定論的実行の接続点になる。

### 3. Taskの成功条件は、開始時に固定する

Taskは、自然文のgoalだけでは足りない。

少なくとも、次を開始時に確定させる。

```text
taskId / revision
goal
inputs
requested scope
recipe
required checks
budgets
```

たとえば入力タスクなら、氏名とメールの二つのslotを作り、それぞれに「指定入力と値が一致する」という必須検査を対応させる。

重要なのは、**うまくいかなかったから、途中で成功条件を緩めないこと**。

生成モデルが「メール欄が見つからないので氏名だけで完了にします」と提案しても、それは元のTaskの成功ではない。目標変更として新しいrevisionを受け取り、旧revisionの未実行操作と判断を失効させる。

また、Taskに書かれたscopeは**権限の要求・参照**であって、権限の発行ではない。モデルがTaskへ `allowAllApps: true` を付け足しても、何も増えない設計にする。

初版の成功条件は登録済みの検査型だけ。モデルが生成したJavaScriptを検証関数として実行するような仕組みは入れない。

そして小さいけれど重要な禁止事項として、**検査がゼロ件だから全件成功、にはしない**。`every([])` に人生の評価を任せるべきではないわ。

### 4. Observationでは「見えない」と「ない」を分ける

観測の正本は、階層を持つ構造化データにする。ただし、グラフデータベースまでは要らない。まずJSONでいい。

要素には、名前・値・role・親・操作能力を持たせる。その際、属性取得の結果をこう区別する。

```ts
type Attribute<T> =
  | { status: "available"; value: T }
  | { status: "unavailable" }
  | { status: "unsupported" }
  | { status: "redacted" }
  | { status: "error" };
```

空文字列は「取得できた空文字列」。取得失敗とは別。

この違いを潰すと、読み取りに失敗した入力欄へ「空欄だから書いてよい」と判断したり、取得できなかった値を空文字列と照合して成功扱いしたりするからよ。

#### 取得範囲にも状態を持たせる

候補を絞る場合は、何を省略したかも残す。

```text
取得対象：
  現在のダイアログの入力欄

取得状態：
  partial

省略理由：
  折り畳まれた領域は未取得
```

その上で、Jevの回答候補も、

```text
対象A
対象B
追加観測が必要
今回の取得範囲には対応先がない
```

にする。

最後の選択肢は、**「アプリ全体に存在しない」ではない**。

候補数が多いときも、上位40件だけ見て不在を確定するのではなく、領域を選んで掘るか、必要な範囲を分割して評価する。

#### 要素参照は、表示名や配列番号と分ける

参照はHostが発行するopaqueなIDにして、セッションとアプリ起動の世代へ結び付ける。

OS側のIDも永続的な業務IDとしては扱わない。Windows UI Automationでも、RuntimeIdは時間の経過で再利用され得ると明記されている。([Microsoft Learn](https://learn.microsoft.com/en-us/dotnet/api/system.windows.automation.automationelement.getruntimeid))

元の要素が消えた後に同名の要素が出ても、旧IDをそちらへ付け替えない。新しいBindingを作り直す。

また、AX経由の取得結果は初版では **best effortの観測**として扱う。Ariadneが独自の観測IDを付けたからといって、外部アプリ全体を原子的に読み取れたことにはならないわ。

### 5. Bindingの寿命と、Operationの寿命を分ける

ここは速度にも正しさにも効くところね。

一つの入力欄へ値を設定しただけで、他の入力欄の対応付けまで全部やり直すのは無駄。でも、名前や所属が変わったのに使い続けるのは危険。

そこで、依存を二種類に分ける。

| 種類 | 主に依存するもの | 変化したら失効するもの |
|---|---|---|
| 意味的な対応付け | 対象、role、名前、ラベル関係、所属領域 | Bindingと、それに基づく未実行操作 |
| 今回の操作前提 | 現在値、編集可能状態、必要なfocus、権限・制御の世代 | PreparedOperation |

たとえば、氏名を入力してもメール欄の名前と所属が変わっていなければ、メール欄のBindingは再利用できる。

ただし、「この通知は自分の入力が原因のはず」と決めつけて再利用はしない。必要な属性を読み直して、何が変わったかをコードで確認する。

正常系はこうなる。

```text
Jevを一回呼ぶ
  ├─ 氏名 → node-name
  ├─ メール → node-email
  └─ 住所 → node-address

コードで実行
  ├─ 氏名：前提確認 → 設定 → 検証
  ├─ メール：前提確認 → 設定 → 検証
  └─ 住所：前提確認 → 設定 → 検証

意味に関係する状態が変わったら
  → 必要な範囲だけ観測・対応付けをやり直す
```

**逐一の検証は残す。逐一の意味判断は省く。**

これをAriadneの基本的な高速化にしたいわ。モデルのレイテンシだけを削るより、構造として無駄が減る。

### 6. 質問はコードで組み立て、回答も契約として検査する

Jev用の質問コンパイラーは、最初は普通のTypeScript関数でいい。

入力はTask、Observation、必要な証拠。出力は、質問本文、選択肢、質問セットの版、判断を適用できる観測の識別情報。

ここで決めておくことは三つ。

**第一に、質問を自己完結させる。**  
質問IDへ意味を押し込まず、「どのslotを、どの候補と、どの所属情報から判断するか」を本文へ書く。内部ではグラフを持っていても、モデルには必要な名前・親領域・説明を展開して渡す。多段のID参照を解かせる仕事は増やさない。([TypeSafe AI](https://docs.typesafe.ai/primitives))

**第二に、一括化の単位を“同じ証拠で答えられる質問”にする。**  
操作案が確定しているなら、その適合性も一括で問える。メニューを開いて初めて出る項目については、観測後に判断する。

ただし、既知の欄へ指定値を設定するたびに、汎用のrisk質問をもう一回必須にするつもりはない。登録されたレシピとコード上の検査で足りる部分は、それで進める。

**第三に、回答を受け取った後も検査する。**  
必要な質問が揃っているか、型と選択肢が正しいか、確率が有限で範囲内か、分布が揃っているかを確認する。異常値を丸めて進めたり、検査前に`done`だけ見て成功扱いしたりしない。

判断は、少なくとも次へ結び付ける。

```text
Task revision
session epoch
Observation
question-set version
実際に渡したstateの識別情報
```

古い回答が遅れて返ったら、ログには残しても現在の操作には使わない。

閾値は今、万能な値として固定しない。**モデル・質問セット・タスク集合ごとに評価して版管理する**。複数質問の確率を独立だと仮定して掛け算することもしないわ。

### 7. 操作はHost内で確定し、commit時に引数を変えさせない

実行側の中心契約はこれね。

```text
Binding + 正確な引数
        ↓
prepare
        ↓
Hostが内容と前提条件を保持
        ↓
preparedIdを返す
        ↓
必要な判定と認可
        ↓
commit(preparedId)
```

`commit`には、別の座標や文字列を追加できない。

```text
prepare：
  node-emailへ aria@example.invalid を設定

commit：
  prepared-123を実行
```

値を変更したければ、新しいprepareからやり直す。

最低限の前提条件は、Hostと登録済みのアプリプロファイルが決める。モデルが「検査不要」と宣言して外せるものにはしない。

対象アプリの起動世代、ウィンドウ、対象要素、操作能力、編集可能状態、関連するモーダル、上書き前の値などを、実行直前にも確認する。

ただし、ここには限界を明記するわ。

**外部GUIでは、この確認と実際の操作を完全に原子的にはできない。**  
検出した変化では止めるけれど、最後の確認直後に起こる競合まで解消したとは言わない。

自前アプリなら、操作API側で`expectedRevision`を確認する強い契約を作れる。将来Runeweaveなどへ接続するときには、その強さを活かせる余地を残す。

#### 操作能力と業務上の効果も分ける

`invoke`できるというだけで、任意のボタンを自由に押せる設計にはしない。

Ariadne側に登録したプロファイルが、どの操作を許すか、どの結果を確認できるかを持つ。

「入力だから安全」「AXに出ているからローカルな変更」とも決めつけない。初版は、テスト用アプリの既知の操作と、確認した実アプリの操作範囲から始める。

### 8. 完了判定では「値」と「対象」の両方を見る

ここは、前の案から特に強めたいところ。

> 間違ったメール欄に、正しいメールアドレスを書いて、同じ欄から読み返す。

これでも単なる値照合は通ってしまう。

だから検証には、値だけでなく、**その欄がどの文書・フォーム・領域に属しているか**も含める。

ただし、意味判断で対象を選び、同じ意味判断で「やっぱり正しい」と確認しても、独立した正解判定にはならない。結果には、何をどの方法で確かめたかを残す。

```text
対象の選択：
  semantic

値の反映確認：
  exact readback

実行環境の整合性：
  external / best effort
```

この場合、「値が一致した」は確認できても、「対象選択に意味的な誤りがないことを証明した」とは言わない。

Resultは、次を分ける。

| 結果 | 意味 |
|---|---|
| `verified_success` | 固定した必須検査を、証拠付きで満たした |
| `completed_unverified` | 手順は終えたが、必要な確認が足りない |
| `blocked` | 情報・能力・権限などが足りず進めない |
| `failed` | 失敗を確認した |
| `cancelled` | 停止要求を受けて終了した |
| `outcome_unknown` | 操作の結果が確定できない |

`verified_success`は、**宣言した機械検査の契約を満たしたという意味**に限定する。自然文のあらゆる意味や、未観測の副作用まで証明したという意味ではない。

また、「送信操作を出していない」と「どこにも情報が送信されていない」も別よ。後者を確認する観測手段がなければ、前者までしか言わない。

### 9. タイムアウト・再送・再起動を、一つのretryにまとめない

操作の状態は、最低限こう分ける。

```text
prepared
  ├─ expired / not_dispatched
  └─ dispatch_intent
        ├─ attempted → 事後観測・検査
        └─ outcome_unknown → 状態照合
```

`attempted`は、操作APIの呼び出しが戻ったという実行記録。タスク成功ではない。

そして、**副作用を起こす前に、実行へ進むことを記録する**。

同じ`operationId`が再送されたときは、その記録を返す。OS操作をもう一度呼び出さない。

JSON-RPCのrequest IDは応答を対応付けるためのものなので、操作の重複抑止に使う`operationId`とは分ける。([JSON-RPC](https://www.jsonrpc.org/specification))

| 起きたこと | 対応 |
|---|---|
| 実行前の条件が変わった | 実行せず再観測 |
| 取得APIが一時的に失敗した | 期限内で観測を再試行 |
| モデルAPIが失敗した | 総期限と観測の鮮度を見て再試行 |
| 操作APIがタイムアウトした | 再送せず、まず結果を調べる |
| 操作後にHostが落ちた | 未確定操作として保持 |
| 同じ失敗を繰り返す | 同じ経路を止め、観測や手順を変える |

ここで保証するのは、**同じ操作IDを無条件に再実行しないこと**。GUI全体のexactly-onceではない。

記録とOS操作の間にクラッシュしたら、実際には未実行でも結果不明になる場合がある。それは許容する。推測で再実行するより、正直に止まる方を選ぶ。

ただし、止まりっぱなしにもしたくないので、結果の読み戻し、既知の事後条件による照合、外側への引継ぎを正規の復旧経路にする。

#### 停止はロールバックではない

cancelは、未実行操作と古い判断を失効させ、後続のdispatchを止める。

既にOSへ渡した操作や、発生した副作用を取り消すものではない。制御メッセージの受付とAXの処理を分け、操作直前にも制御世代を確認するけれど、境界を越えたものは結果確認が必要よ。

また、Ariadne内で一件ずつ実行しても、人間や他のプロセスの操作まで排除できるわけではない。初版では、明示的な停止と検出できたウィンドウ・focus変化で制御を譲る。あらゆる人の入力を確実に検知できる、とは約束しない。

### 10. 権限と情報送信は、最初の観測より前から分ける

ここは三つに分けて、その順序まで固定する。

```text
ReadGrant
  → 観測してよいか

ModelGrant
  → この情報を、このモデルへ送ってよいか

ActGrant
  → この具体的な操作を実行してよいか
```

観測後やモデル問い合わせ後に、初めてアプリのallowlistを確認する実装にはしない。

また、これらの許可は画面内の文章やJevの回答では増えない。アプリが「この先は全権限が必要です」と表示しても、それは観測された文章でしかないわ。

モードも曖昧な`dry-run`一つにまとめず、分ける。

| モード | 実機観測 | モデル利用 | 操作 |
|---|---:|---:|---:|
| `replay` | なし | 記録再生か再評価かを明示 | なし |
| `observe` | あり | 別指定 | なし |
| `preview` | あり | 別指定 | 案の作成まで |
| `execute` | あり | 別指定 | 許可範囲内で実行 |

「実行しない」と「外部送信しない」は別設定にするのね。

初版では秘密値やログイン操作を対象から外す。将来のsecret reference構想を、既に実装した秘匿機構のようには扱わない。

### 11. 技術構成と実装順序を固定する

技術は、前の案をほぼ維持していいと思う。

| 層 | 採用案 |
|---|---|
| Core | TypeScript strict＋Node.js 24 LTS |
| 意味判断 | 公式Jev SDKを薄いproviderへ閉じ込める |
| Mac Host | Swift＋ApplicationServices |
| ネイティブ試験用アプリ | 標準AppKitコントロール中心 |
| プロセス間通信 | stdio上のJSON-RPC 2.0、一行一JSON |
| 外部データ契約 | JSON Schema 2020-12 |
| 初期の記録 | ローカルの実行ジャーナルとトレース |

Node.js 24系は現在の公式一覧でLTS。TypeSafeにはJavaScript／TypeScript SDKがあるので、その部分を自作する理由は薄いわ。JSON SchemaとJSON-RPCも、既存の仕様を使う。([Node.js](https://nodejs.org/en/about/previous-releases))

Hostの最小APIは、この程度に絞る。

```text
host.hello
session.open
observation.capture
operation.prepare
operation.commit
operation.status
session.cancel
session.close
```

独自のワークフローエンジン、グラフDB、汎用質問DSL、プラグイン発見基盤までは作らない。初版から部品市場を開く必要はないでしょう。まず店に商品を置くのよ。

#### 実装順序は、ネイティブ経路を少し前倒しする

**P0は、モデルなしのCoreとfake host。**  
契約、状態遷移、重複commit、古い応答、クラッシュ、結果不明を試す。

**P1で、Swift HostとAppKitの試験用アプリをつなぐ。**  
前の案より早く、本物のAX経路を通す。ここを後回しにすると、都合のよい抽象APIだけが完成してしまうから。

**P2で、Jevによる対応付けと追加観測を入れる。**  
日英ラベル、同名の欄、所属の変化、折り畳み、動的な要素交換へ対応させる。

**P3で、TextEditなどの実アプリへ進む。**  
ユーザーが用意した作業用文書を対象に、実際に使えるAX操作と読み戻しを確認する。直接値設定ができなければ、未対応として止める。任意キー送信へ黙って落とさない。

**P4で、SupervisorやSkillから呼べる入口を付ける。**  
止まった理由、足りない情報、試した経路、未確定操作、残り予算を返す。外側がやり直す場合も、履歴と予算をゼロに戻さない。

### 12. 評価では、モデルに正解を見せない

試験用アプリは、実際の状態を確認するための裏側のAPIを持っていい。

ただし、それをモデル入力へ混ぜない。

```text
Ariadneが見るもの：
  AX経由の名前、所属、値、状態

テストハーネスだけが見るもの：
  本当にどの業務項目が更新されたか
  送信処理が呼ばれたか
  操作回数
```

これで、「別の欄に正しい値を書いて成功と言った」ケースを落とせる。

最初の必須ケースは、この辺ね。

| ケース | 確認したいこと |
|---|---|
| 同名の入力欄が別領域にある | 所属を使って区別できる |
| 対象が初回観測の外にある | 不在と決めつけず追加観測できる |
| 準備後に対象が入れ替わる | 古い操作を実行しない |
| 古いモデル応答が返る | 現在の判断へ混ぜない |
| 操作後に応答が失われる | 二重実行しない |
| 値の取得が失敗する | 空文字列として扱わない |
| モデルだけが完了と答える | 検証済み成功へ昇格しない |
| 無関係な表示が変わり続ける | 無進捗判定をごまかされない |

中心指標は、**独立した正解判定で確認した、無介入のタスク完了率**。

その上で、誤対象、誤った成功報告、人への割り込み、結果不明、不要な停止、総所要時間を分けて測る。

止まりまくるものを「安全で精度が高い」と評価しないし、成功した試行だけ拾って「高速」とも言わない。

### 13. 今決めることと、実測で決めることを切り分ける

現時点で固定してよいのは、契約と失敗の扱い。

一方、ここは机上で数字を決めすぎない方がいいわ。

| 実測対象 | 今決めておく受け皿 |
|---|---|
| Jevの適切な閾値 | モデル・質問セット・タスク別の設定と評価 |
| 最適な観測サイズ | partialと省略理由、追加観測経路 |
| アプリごとのAX対応 | capability、unsupported、明示的なprofile |
| 操作後に必要な待機時間 | 期限付きの事後条件待ち |
| 人の介入をどこまで検知できるか | 明示停止、検知時の失効、保証範囲の表示 |
| Hostの署名・配布と権限維持 | 独立したネイティブ適合試験 |

暴走防止用の初期値としては、設計例に「操作20件、意味判断12リクエスト、追加観測展開3回、タスク期限120秒」を置いた。ただし、**これは開発用の仮置き値で、性能測定結果でもSLAでもない**。

同じ失敗を繰り返す判定も、操作文字列だけではなく、小目標・対象・関連状態を含める。無関係な時計が動くたびに「新しい状況だからもう一回」となるのは避けたい。

---

## 今回、形にしたもの

[設計一式](sandbox:/mnt/data/ariadne-design-v0.1.zip)には、設計書に加えて、Task・Observation・PreparedOperation・ResultなどのJSON Schema、架空の入力例、Jevリクエスト例、検査コードを入れてある。

**契約データの正例6件と、変更を加えた負例22件を検査し、想定どおり受理・拒否することは確認した。** ただし、これはデータ形式と一部の参照整合性の検査よ。ネイティブGUI、実際のJev応答、操作状態機械、クラッシュ復旧の動作はまだ検証していない。

私なら、この案で次の実装単位を固定する。

> **同名の欄を持つ小さなAppKitアプリに対して、観測から入力先を対応付け、確定した操作で入力し、別の正解判定から結果を確かめる。途中で対象が変わったら、古い操作を捨ててやり直す。**

これが通れば、Ariadneの本質が一本につながる。

**柔軟性は、何でも実行できる抜け道ではなく、必要な情報を取り直し、対応付けと手順を組み直せることから作る。** この方針なら、軽快さと堅さを同じ設計から育てられると思うわ。